# LINE Mini Dapp Configuration
NEXT_PUBLIC_MINIAPP_ENABLED=true
NEXT_PUBLIC_MINIAPP_APP_ID=      # LINE Mini Dapp App ID

# LIFF ID - Required for LINE Mini App
NEXT_PUBLIC_LIFF_ID=YOUR_LIFF_ID
# How to obtain:
# 1. Go to LINE Developers Console: https://developers.line.biz/console/
# 2. Create or select your LINE channel
# 3. Go to LIFF tab and create a new LIFF app
# 4. Copy the LIFF ID from the created app
#
# LIFF Settings Requirements:
# - Module mode: OFF (to enable Action Button)
# - Enable LIFF Action Button: ON
# - Enable Minimization: ON
# - Required scopes: openid, profile

# Mini Dapp Client ID - Required for wallet functionality
NEXT_PUBLIC_DAPP_CLIENT_ID=YOUR_MINI_DAPP_CLIENT_ID
# How to obtain:
# 1. Contact the Dapp Portal team for Mini Dapp SDK authorization
# 2. Sign the contract and agree to Terms & Conditions
# 3. They will email you the clientId
#
# IMPORTANT Security Notes:
# - NEVER commit actual values to version control
# - Store ONLY the clientId in frontend
# - If you receive a clientSecret (for payments), keep it server-side ONLY
# - If clientSecret is leaked, contact Dapp Portal team immediately to rotate it

# KAIA RPC (testnet or mainnet)
NEXT_PUBLIC_KAIA_RPC_URL=
# ETH RPC for read-only strategy stats
NEXT_PUBLIC_ETH_RPC_URL=

# Contracts (addresses)
NEXT_PUBLIC_KAIA_USDT=          # KAIA USDT (ERC20, faucet-capable on test)
NEXT_PUBLIC_KAIA_RKUSDT=        # rkUSDT (ERC20)
NEXT_PUBLIC_KAIA_VAULT=         # ReKaUSD Vault (deposit/withdraw)
NEXT_PUBLIC_ETH_STRATEGY=       # ETH-side strategy
NEXT_PUBLIC_ETH_USDT=           # (optional) strategy-held USDT address
NEXT_PUBLIC_ETH_SUSDE=          # (optional) sUSDe address

# Permit2 Address (KAIA Kairos testnet)
NEXT_PUBLIC_KAIA_PERMIT2=0x000000000022d473030f116ddee9f6b43ac78ba3

# Chain Configuration
# Use '1001' for Baobab testnet (default for development)
# Use '8217' for Cypress mainnet (production)
NEXT_PUBLIC_KAIA_CHAIN_ID=1001
NEXT_PUBLIC_ETH_CHAIN_ID=11155111

# Testing in LINE App:
# 1. Open LINE app on your mobile device
# 2. Navigate to your LIFF URL: https://liff.line.me/{YOUR_LIFF_ID}
# 3. The app should initialize LIFF first, then Mini Dapp SDK
# 4. Wallet connection should only trigger on user action (not auto-connect)
# 5. Test LIFF actions (share/minimize) using the floating action buttons